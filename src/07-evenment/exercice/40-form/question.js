/**
 * Exercice - form
 *
 * Expérimentation avec les évènements associée au formulaire
 *
 * Consignes:
 *  Créer le fichier js/form.js à partir du code suivant.
 *      - Utiliser l'évènement submit pour valider le formulaire
 *      - Si le formulaire n'est pas valide, stopper l'envoi et afficher les messages d'erreurs.
 *      - Si le formulaire est valide, ouvrir une fenêtre modale en utilisant la commande alert()
 */
'use strict'

const PRODUCT_NUMBER_REQUIRED = 'Le numéro du produit est un champ obligatoire'
const PRODUCT_NUMBER_LENGTH = 'Le numéro de produit doit être constitué de 5 caractères maximum'
const PRODUCT_PRICE_REQUIRED = 'Le prix du produit est un champ obligatoire'

function validateNumber () {
    const liElements = []

    // ajouter la validation nécessaire pour créer la collection de LI contenant les messages

    return liElements
}

function validatePrice () {
    const liElements = []

    // ajouter la validation nécessaire pour créer la collection de LI contenant les messages

    return liElements
}
