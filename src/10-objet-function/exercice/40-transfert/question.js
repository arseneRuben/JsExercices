/**
 * Exercice - Transfert
 *
 * Révision portant sur les évènements et la manipulation du DOM.
 * Plusieurs paramètres sont fournis lors de l'initialisation du module, ce qui permet de créer un module réutilisable.
 *
 * Rappel:
 *  L'usage de Element.appendChild() permet d'insérer un Node dans un conteneur.
 *  Puisque qu'un élément ne peut exister simultanément a 2 endroits, il sera donc retiré de sa position initiale s'il était déjà dans le DOM.
 *
 * Consignes:
 *  - Créer le fichier js/transfert.js
 *  - Initialisé la liste d'usager en utilisant la collection d'usagers fournie en paramètre.
 *  - Ajouter les balises h2 au-dessus de chaque liste en utilisant les messages fournit en paramètre.
 *  - Ajouter les évènements nécessaires pour permettre le transfert des balises LI d'une liste a l'autre.
 *  - Le HTML et CSS ne doivent pas être modifiés.
 *  - Le fichier transfert.js ne doit pas contenir d’ID, CLASS ou message écrit en dure (Hard codé).
 *
 */
'use strict'
